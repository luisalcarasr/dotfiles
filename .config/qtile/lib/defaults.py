from utils.theme import colors

defaults = dict(
    font='Cantarell Bold',
    fontsize=14,
    padding=8,
    borderwidth=0,
    foreground=colors["white"]
)

nerd_font = 'BlexMono Nerd Font'

mod = "mod4"
